# Backlog

This folder contains tasks (one per file) which need to be implemented, it does not contain tasks which are already closed.


Once a task is implemnted, the task definition file is moved to `/work/closed/`
